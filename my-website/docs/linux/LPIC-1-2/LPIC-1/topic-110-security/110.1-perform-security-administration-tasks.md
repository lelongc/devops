# 🐧 Objective 110.1: Quản Trị Bảo Mật Hệ Thống & Sudo Audit (LPI 102-500) - Bản Chất Hệ Thống & Thực Chiến DevOps

> 💡 **Bản chất 1 câu**: Rà soát lỗ hổng SUID/SGID, quản lý chính sách tuổi mật khẩu (`chage`), tăng giới hạn tài nguyên (`ulimit`/`limits.conf`), cấu hình `sudo` với `visudo` và quản lý người dùng đang đăng nhập (`w`, `who`, `last`).  
> 🎯 **Trọng tâm phỏng vấn DevOps**: Audit SUID/SGID (find / -perm -4000), chage (password aging), ulimit / limits.conf (nofile/nproc), cấu hình sudo (/etc/sudoers, visudo), giám sát user (w, who, last, pkill).

---

## 1. 🧠 Hình Hình Dung Nhanh (Intuitive Mindset)

Quản trị bảo mật Linux Server như quản lý tòa nhà văn phòng: Rà soát cổng thoát hiểm (SUID Audit), bắt buộc đổi thẻ từ theo định kỳ (chage), cài đặt số lượng người tối đa trong thang máy (ulimit) và cấp quyền truy cập phòng VIP bằng sổ đăng ký bảo vệ (sudoers).

---

## 2. 📚 Lý Thuyết Chuyên Sâu & Bản Chất Hệ Thống (Under The Hood Mechanics - LPI Official 101 & 102)

### 2.1 Các Tác Vụ Quản Trị Bảo Mật Cốt Lõi (Security Administration)

1. **Rà Soát File SUID / SGID Nguy Hiểm**:
   - File SUID (Bit 4000) thực thi với quyền của Owner (root). Nếu file thực thi bị khai thác chèn mã độc, Hacker có thể chiếm quyền Root lập tức.
   - Lệnh audit: `find / -perm -4000 -type f 2>/dev/null` (SUID) hoặc `find / -perm -2000 -type f 2>/dev/null` (SGID).

2. **Quản Lý Tuổi Thọ Mật Khẩu (Password Aging Policy)**:
   - Xem thông tin tuổi mật khẩu: `chage -l username`.
   - Các tham số chính:
     - `-M 90`: Mật khẩu hết hạn sau 90 ngày.
     - `-m 7`: Tối thiểu 7 ngày mới được đổi pass lại.
     - `-W 14`: Cảnh báo trước 14 ngày khi pass sắp hết hạn.
     - `-E YYYY-MM-DD`: Hết hạn tài khoản vào ngày chỉ định.

3. **Giới Hạn Tài Nguyên Tiến Trình (Process Resource Limits - ulimit)**:
   - Cấu hình Soft & Hard limit trong `/etc/security/limits.conf`.
   - Các chỉ số quan trọng cho Server chịu tải lớn (Nginx, Node.js, Database):
     - `nofile`: Số lượng File Descriptors mở tối đa (mặc định 1024 -> tăng lên 65535).
     - `nproc`: Số lượng tiến trình tối đa một user được tạo.

4. **Cấu Hình Cấp Quyền Sudo An Toàn (`/etc/sudoers` & `visudo`)**:
   - BẮT BUỘC dùng lệnh `visudo` để chỉnh sửa `/etc/sudoers` (tự động kiểm tra cú pháp tránh khóa mất quyền root).
   - Cú pháp: `user HOST=(RUNAS_USER:RUNAS_GROUP) COMMANDS`.
   - Cho phép nhóm devops chạy sudo không cần nhập pass: `%devops ALL=(ALL) NOPASSWD: ALL`.

5. **Giám Sát & Ngắt Phiên Đăng Nhập Người Dùng (Session Audit)**:
   - `w` / `who`: Xem ai đang đăng nhập và đang chạy lệnh gì.
   - `last`: Xem lịch sử đăng nhập vào hệ thống từ `/var/log/wtmp`.
   - `pkill -u username` hoặc `pkill -9 -t pts/0`: Đá phiên làm việc rủi ro ra khỏi Server.


---


### 2.4 Cơ Chế Hoạt Động Bên Dưới Kernel & Kiến Trúc Hệ Thống Chi Tiết (Deep Kernel & Architecture)
- **Tầng Giao Tiếp System Calls**: Mọi thao tác người dùng hay lệnh CLI gõ trên Terminal đều trải qua giao diện System Call Interface (như `open()`, `read()`, `write()`, `fork()`, `execve()`, `socket()`, `epoll_wait()`) để chuyển quyền điều khiển từ User Space (Ring 3) xuống Kernel Space (Ring 0).
- **Quản Lý Tài Nguyên & Cấu Trúc Dữ Liệu**: Kernel duy trì các cấu trúc dữ liệu bảng điều khiển (Process Control Block - PCB, File Descriptor Table, Inode Index Table, Page Tables) để đảm bảo cô lập tài nguyên, an toàn bộ nhớ và tối ưu hiệu năng I/O.
- **Tương Tác Phần Cứng & Drivers**: Các trình điều khiển thiết bị (Kernel Modules `.ko`) trực tiếp tương tác với thanh ghi phần cứng (Hardware Registers) qua ngắt CPU (Interrupts - IRQ) và truy cập bộ nhớ trực tiếp (DMA - Direct Memory Access).

---

## 3. ⚡ Bảng Tra Cứu Câu Lệnh Thực Hành (CLI Command Reference)

| Câu lệnh | Tham số (Options/Flags) | Giải thích ý nghĩa chi tiết | Ví dụ thực tế trong DevOps |
| :--- | :--- | :--- | :--- |
| **`visudo`** | `Mở file /etc/sudoers sửa quyền sudo an toàn có check syntax` | Không | `sudo visudo` |
| **`chage`** | `Xem và thiết lập chính sách tuổi thọ mật khẩu` | -l, -M, -W | `sudo chage -M 90 devops` |
| **`ulimit`** | `Xem và đặt giới hạn tài nguyên Shell (file/process)` | -a, -n, -u | `ulimit -n 65535` |
| **`w / who / last`** | `Kiểm tra danh sách và lịch sử người dùng đăng nhập` | Không | `w` |
| **`pkill`** | `Hủy các tiến trình hoặc phiên đăng nhập của user` | -9 -u, -t | `sudo pkill -9 -u hacker` |


---

## 4. 🛠 Thao Tác Thực Chiến & Kịch Bản DevOps (Real-World Scenarios)

### 🛠 Các lệnh thực hành gõ là ăn ngay:
```bash
# 1. Tăng ulimit nofile cho Nginx/Docker trong /etc/security/limits.conf:
cat << 'EOF' | sudo tee -a /etc/security/limits.conf
* soft nofile 65535
* hard nofile 65535
* soft nproc 65535
* hard nproc 65535
EOF

# 2. Cấp quyền sudo NOPASSWD cho group devops qua visudo:
echo '%devops ALL=(ALL) NOPASSWD: ALL' | sudo tee /etc/sudoers.d/devops
```

### 🚀 Kịch bản xử lý sự cố thực tế khi đi làm:
Rà soát sự cố nghi vấn bị xâm nhập hệ thống:
# 1. Kiểm tra người dùng đang đăng nhập:
w
# 2. Xem lịch sử login:
last | head -n 20
# 3. Đá phiên đăng nhập rủi ro tại pts/1:
sudo pkill -9 -t pts/1

---


### 4.2 Chi Tiết Các Lỗi Thường Gặp & Kịch Bản Khắc Phục Lỗi (Troubleshooting Deep-Dive)
1. **Sự cố 1: Lỗi Cạn Kiệt Tài Nguyên & Nghẽn Cổ Chai (Resource Exhaustion / Bottleneck)**:
   - **Triệu chứng**: Hệ thống phản hồi siêu chậm, ứng dụng bị crash OOM (Out Of Memory) hoặc lệnh báo `No space left on device` dù đĩa còn dung lượng trống.
   - **Bản chất nguyên nhân**: Cạn kiệt Inodes đĩa cứng (`df -i`), tràn bộ nhớ đệm RAM (`free -h`), hoặc cạn kiệt File Descriptors tối đa (`sysctl fs.file-max`).
   - **Các bước xử lý khẩn cấp**:
     ```bash
     # 1. Kiểm tra số lượng Inodes khả dụng trên đĩa:
     df -i /
     
     # 2. Tìm thư mục chứa hàng triệu file rác gây hết Inodes:
     find /var/spool /tmp -xdev -type f | cut -d/ -f2-4 | sort | uniq -c | sort -nr | head -n 10
     
     # 3. Kiểm tra các tiến trình đang chiếm giữ file đã bị xóa (Deleted Descriptor Leak):
     sudo lsof | grep deleted
     
     # 4. Giải phóng bộ nhớ RAM Cache tạm thời của Linux Kernel:
     sudo sysctl -w vm.drop_caches=3
     ```

2. **Sự cố 2: Lỗi Sai Cấu Hình & Tự Động Phục Hồi (Configuration Misconfiguration & Recovery)**:
   - **Triệu chứng**: Dịch vụ ngắt đột ngột, không kết nối được qua cổng mạng (Port Unreachable) hoặc lệnh service return exit code != 0.
   - **Các bước xử lý khẩn cấp**:
     ```bash
     # 1. Kiểm tra cú pháp file cấu hình trước khi restart service:
     sudo systemctl status <service_name> --no-pager -l
     
     # 2. Xem log chi tiết lỗi khởi động từ Journald binary log:
     sudo journalctl -u <service_name> -n 100 --no-pager -e
     
     # 3. Phân tích lời gọi hệ thống Syscall xem tiến trình bị treo ở đâu bằng strace:
     sudo strace -p <PID> -f -e trace=file,network
     ```

---

## 5. 🚀 Bộ Câu Hỏi Phỏng Vấn DevOps Thực Tế (Interview Q&A)

> **Q: Tại sao BẮT BUỘC phải dùng lệnh `visudo` thay vì sửa trực tiếp `/etc/sudoers`?**  
> **A**: Vì `visudo` tự động kiểm tra lỗi cú pháp (Syntax Check) trước khi lưu. Sửa lỗi trực tiếp `/etc/sudoers` có thể làm hỏng quyền sudo làm khóa hoàn toàn Server.

> **Q: Lệnh nào đặt số lượng File Descriptors mở tối đa lên 65535 cho phiên làm việc hiện tại?**  
> **A**: Lệnh `ulimit -n 65535`.

> **Q: Lệnh nào audit danh sách các file mang quyền SUID trên hệ thống?**  
> **A**: Lệnh `find / -perm -4000 -type f 2>/dev/null`.





> **Q: Làm thế nào để điều tra và xử lý tận gốc sự cố Linux Server báo lỗi "No space left on device" trong khi lệnh `df -h` cho thấy dung lượng đĩa vẫn còn trống 40%?**  
> **A**: Lỗi này thường do 2 nguyên nhân cốt lõi:  
> 1. **Hết Inode (Inode Exhaustion)**: File system đã dùng hết số lượng bảng Inode để lưu trữ metadata file (do có hàng triệu file kích thước siêu nhỏ 0-byte). Kiểm tra bằng `df -i`. Xử lý bằng cách tìm và xóa các thư mục chứa file rác (như `/var/spool/postfix/maildrop` hay `/tmp`).  
> 2. **File Descriptor Leak (File đã xóa nhưng tiến trình vẫn đang mở)**: Một file dung lượng lớn bị xóa bằng lệnh `rm` nhưng tiến trình ứng dụng vẫn đang giữ file handle mở, khiến Kernel chưa thể giải phóng dung lượng đĩa thực sự. Kiểm tra bằng `sudo lsof | grep deleted`, sau đó restart lại tiến trình giữ file đó để Kernel thu hồi đĩa.

> **Q: Giải thích sự khác biệt giữa hai tín hiệu ngắt `SIGTERM` (15) và `SIGKILL` (9) trong Linux OS? Khi nào nên dùng loại nào trong kịch bản tự động hóa DevOps?**  
> **A**:  
> - **`SIGTERM` (Signal 15)**: Tín hiệu yêu cầu dừng ứng dụng **thỏa thuận (Graceful Termination)**. Tiến trình có thể bắt (catch) tín hiệu này để đóng kết nối Database, lưu trạng thái dữ liệu và dọn dẹp tài nguyên trước khi thoát hoàn toàn. Luôn luôn ưu tiên dùng `SIGTERM` trước (như trong Docker/Kubernetes shutdown flow).  
> - **`SIGKILL` (Signal 9)**: Tín hiệu dừng ứng dụng **cưỡng chế tuyệt đối (Forced Termination)** do Kernel trực tiếp tiêu diệt tiến trình ngay lập tức. Tiến trình KHÔNG THỂ bắt hay lờ đi tín hiệu này, dẫn tới nguy cơ hỏng dữ liệu dở dang. Chỉ dùng `SIGKILL` khi tiến trình bị treo đơ không phản hồi `SIGTERM`.

---

## 6. 📝 Cheat Sheet & Ghi Nhớ Nhanh (Summary)

- [x] SUID Audit: find / -perm -4000
- [x] chage -M 90: Đặt hạn pass 90 ngày
- [x] ulimit -n 65535: Tăng giới hạn file mở
- [x] visudo: Sửa /etc/sudoers an toàn
- [x] w / last: Kiểm tra user đăng nhập


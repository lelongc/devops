# kubectl config
kubectl config view
# kubectl get nodes
kubectl get nodes 
# all command relate to config k8s
aws eks update-kubeconfig --region .... --name cluster-name
# view kubectl config
kubectl config view
# get nodes
kubectl get nodes
# current context
kubectl config current-context
# apply all the manifest 
kubectl apply -f complete.yml